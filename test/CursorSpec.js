const FSTree = require('../file-system-tree')
const FSTNode = require('../tree-node')
const TreeCursor = require('../tree-cursor')
const assert = require('assert')

describe("a cursor", () => {
  it("could be creatable", () => {
      return assert.equal(1, 1, "oops")
  })
})
